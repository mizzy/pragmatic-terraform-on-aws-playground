{
    "schemaVersion": "1.0",
    "description" : "Document to hold regional settings for Session Manager",
    "sessionType": "Standard_Stream",
    "inputs": {
        "s3BucketName": "${aws_s3_bucket.operations.id}",
        "cloudWatchLogGroupName": "${aws_cloudwatch_log_group.operation.name}"
    }
}
